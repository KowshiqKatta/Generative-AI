fix(agent): tell the retrieval agent which documents are loaded

Routing was fixed but the agent still chose web_search over the vector
store for "who is <name>" while a document about that person was loaded.
RETRIEVE_SYSTEM instructs "questions about the uploaded papers -> vector
store" without ever naming the uploaded papers, so the rule was unusable.

- agent_node injects the loaded titles with an explicit ordering rule:
  vector store first when the question could plausibly be answered from a
  loaded document, web only for external or time-sensitive information or
  after retrieval came back empty. States plainly when nothing is loaded.
- Extract _loaded_titles(), shared with router_node.
- The relevancy check could not have caught this: the web results did
  answer the question, so is_relevant was true. It grades whether the
  context answers the query, not whether the source was right.
- Style the delete control via st-key-del_* instead of column position;
  the positional selector was not matching, so the button kept its box.